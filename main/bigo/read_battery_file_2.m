function [t, v] = read_battery_file(file_name)
   %READ_BATTERY_FILE Read a battery-level file
   %
   %   [t, v] = READ_BATTERY_FILE(file_name) parses a csv file that contains
   %   timestamps (in MATLAB notation) and battery level (typically in range of
   %   0.00 - 1.00).
   
   
   
   
end
